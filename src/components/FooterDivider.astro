---
// FooterDivider.astro - SVG divider between content and footer
---

<div class="footer-divider">
  <svg
    xmlns="http://www.w3.org/2000/svg"
    viewBox="0 0 1200 150"
    preserveAspectRatio="none"
  >
    <!-- Background rectangle with main background color -->
    <rect x="0" y="0" width="1200" height="150" fill="var(--color-background)" />
    
    <!-- Black divider lines on top -->
    <!-- Row 1: 40px height -->
    <rect x="0" y="0" width="1200" height="40" fill="#000000" />

    <!-- Row 2: 30px height (gap: 2px) -->
    <rect x="0" y="42" width="1200" height="30" fill="#000000" />

    <!-- Row 3: 20px height (gap: 2.5px) -->
    <rect x="0" y="74.5" width="1200" height="20" fill="#000000" />

    <!-- Row 4: 15px height (gap: 3px) -->
    <rect x="0" y="97.5" width="1200" height="15" fill="#000000" />
    <!-- Row 5: 10px height (gap: 3.5px) -->
    <rect x="0" y="116" width="1200" height="10" fill="#000000" />
    <!-- Row 6: 5px height (gap: 4px) -->
    <rect x="0" y="130" width="1200" height="5" fill="#000000" />
    <!-- Row 7: 2px height (gap: 4.5px) -->
    <rect x="0" y="139.5" width="1200" height="2" fill="#000000" />
    <!-- Row 8: 1px height (gap: 5px) -->
    <rect x="0" y="146.5" width="1200" height="1" fill="#000000" />
  </svg>
</div>

<style>
  .footer-divider {
    position: relative;
    width: 100%;
    overflow: hidden;
    line-height: 0;
    z-index: 3;
  }

  .footer-divider svg {
    position: relative;
    display: block;
    width: calc(100% + 1.3px);
    height: 120px; /* Increased height for the multi-level sine wave */
  }

  html.dark .footer-divider svg path {
    fill: var(--color-background);
  }
</style>
